import MainContainer from '../container/MainContainer';

const main = () => (
    <div>
        <span>this is main router</span>
        <br/>
        <br/>

        <MainContainer/>
    </div>
);

export default main;